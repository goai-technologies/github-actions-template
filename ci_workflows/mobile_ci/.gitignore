# Build artifacts — publish via GitHub Release / Actions Artifacts, not git
*.apk
*.aab
*.ipa
release/*/

# Signing material — NEVER commit
*.p12
*.p8
*.mobileprovision
*.keystore
*.jks
**/keystore.properties
**/play*.json
private_keys/

# Native projects regenerated by Expo prebuild
/ios/
/android/
